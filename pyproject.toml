[tool.poetry]
name = "modal"
version = "0.1.0"
description = ""
authors = ["Your Name <you@example.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.8"
modal-client = "^0.45.1173"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
